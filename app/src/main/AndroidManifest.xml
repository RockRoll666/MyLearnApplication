<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.exercise.tiger.mylearnapplication">

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".base.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.TestRxJavaActivity"/>
        <activity android:name=".activity.TestRetrofitActivity"/>
        <activity android:name=".activity.TestRvWithRetrofitActivity"/>
        <activity android:name=".activity.TestGlideActivity"/>
        <activity android:name=".activity.TestLargeImageActivity"/>
        <activity android:name=".activity.TestScrollImageActivity"/>
        <activity android:name=".activity.TestGestureImageActivity"/>
    </application>
</manifest>